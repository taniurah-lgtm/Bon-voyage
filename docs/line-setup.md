# LINE連携のセットアップ手順(初回のみ・所要15分)

イベントレポートを家族のLINEに届けるために、無料のLINE公式アカウント(Messaging API)を1つ作り、アクセストークンをClaude Codeの環境変数に登録します。

## 1. LINE公式アカウント(Messaging APIチャネル)を作る

1. https://developers.line.biz/ja/ を開き、普段のLINEアカウントでログイン。
2. 「プロバイダー」を新規作成(名前は例: `家族イベント`)。
3. プロバイダー内で「新規チャネル作成」→「Messaging API」を選択。
   - ※2024年以降は先に https://entry.line.biz/ でLINE公式アカウントを作成し、
     LINE Official Account Manager(https://manager.line.biz/)の
     「設定 → Messaging API」から有効化する導線の場合があります。画面の案内に従ってください。
   - チャネル名: 例 `ぼんぼやーじゅ通信`(家族に表示される名前)
   - 業種: 個人 / その他 でOK
4. 作成できたら LINE Developers コンソールでチャネルを開く。

## 2. チャネルアクセストークンを発行する

1. チャネルの「Messaging API設定」タブを開く。
2. 一番下の「チャネルアクセストークン(長期)」の「発行」を押す。
3. 表示された長い文字列をコピーする(これが `LINE_CHANNEL_ACCESS_TOKEN`)。
   ⚠️ このトークンは秘密情報。リポジトリやチャットに貼らないでください。

## 3. 応答設定を整える(任意だが推奨)

LINE Official Account Manager(manager.line.biz)で:
- 「応答設定」→ あいさつメッセージ: オフ or 簡単な文に
- 「応答設定」→ 応答メッセージ: オフ(自動返信の「ありがとうございます!」を止める)
- Webhookは使わないのでオフのままでOK。

## 4. Claude Code の環境変数にトークンを登録する

1. https://claude.ai/code (またはClaudeアプリ)で、このセッションが動いている
   **環境(Environment)の設定**を開く。
2. 環境変数に以下を追加:
   - 名前: `LINE_CHANNEL_ACCESS_TOKEN`
   - 値: 手順2でコピーしたトークン
3. 保存。次回セッション起動(次の巡回)から有効になります。

## 5. 家族が友だち追加する

1. LINE Developers の「Messaging API設定」タブ(またはOfficial Account Manager)に
   QRコードがあるので、家族全員のLINEで友だち追加。
2. レポートは「ブロードキャスト送信」なので、友だち追加した人全員に届きます。

## 6. 動作確認

Claudeに「LINEテスト送信して」と伝えると、`scripts/line_report.sh` でテスト
メッセージを送ります。家族のLINEに届けば完了です。

## 備考

- 無料プランの送信上限は月200通(受信者ごとに1通カウント)。
  週2回 × 家族分でも十分収まりますが、家族5人×週2なら月約45通です。
- トークンを再発行した場合は環境変数を更新してください。
- LINE側からの返信はこの仕組みでは受信できません。イベントの承認
  (カレンダー登録)はClaudeへの返信で行います。

---

## 認証済アカウントの審査に落ちた（2026-09-10）

**受付番号 600332314 / 審査結果: 非承認 / 理由「弊社審査基準に抵触するため」**

LINEはガイドラインで「**当社はお客様に対し、審査結果およびその理由について説明する義務を負わない**」と
明記している。**本当の理由は分からない。**以下は推測であることを忘れない。

### 失ったものは1つだけ

| | 未認証（いま） | 認証済 |
|---|---|---|
| ブロードキャスト配信 | ⭕️ | ⭕️ |
| 友だち追加のQR・URL | ⭕️ | ⭕️ |
| リッチメニュー | ⭕️ | ⭕️ |
| ライトプランの課金 | ⭕️（**無駄になっていない**） | ⭕️ |
| **LINEアプリ内の検索に出る** | ❌ | ⭕️ |
| 販促ポスターの種類 | 1種類 | 3種類 |
| バッジ | グレー | 緑 |

**チラシのQRも、いまの配信も、何ひとつ止まらない。**
減ったのは「LINEの中で名前を検索して見つけてもらう」経路だけ。
読者を増やす道は**紙（20館＋児童館＋園）とThreads**なので、**痛手は小さい**。

### 落ちた理由の見立て（推測）

**実在を確認する材料が足りなかった**、が本命。審査は要するに身元確認で、
LINE側から見えるのは次だけだった。

- 法人ではない（個人）
- **ホームページに代表者名・所在地・電話番号が無い**（`docs/homepage/index.html` の発行者欄はメールのみ）
- あすぴあ登録団体 第254号 — 実在の証明としては本物だが、**LINEが照合できる公的登録ではない**
- 連絡先が Gmail のみ
- 店舗が無い

### いまは追わない

再申請にオーナーの時間を使うより、**保育課への電話と10月の紙**のほうが読者に直結する。
LINE内検索から来る人より、園と地域センターの棚から来る人のほうが多い。

### ✅ 前提がひっくり返った（2026-09-13）

**開業届は、すでに出ている。屋号「ニコマル」／個人事業主は妻。**

上の見立てで「足りない」と書いた**実在の証明は、もう手元にある。**
「有料化のついでに」と後回しにしていたが、**その理由が消えた。**

| 審査に足りなかったと見ていたもの | いま |
|---|---|
| 個人事業主としての実在証明 | ✅ **開業届（屋号ニコマル）がある** |
| 代表者名・所在地 | ✅ 開業届に書いてある（**ホームページに出すかは別の判断**） |
| 特商法の表記 | **不要。**有料商品が無いので、法律上も要らない（2026-09-04 に廃止済み） |

### 再申請するときの注意（1つだけ）

🔴 **開業届の名義は妻。LINE公式アカウントの管理者はオーナー（夫）。**

再申請の事業者欄は、**開業届と一字一句そろえる**:

| 欄 | 入れるもの |
|---|---|
| 事業者名 | **ニコマル**（屋号。開業届のとおり） |
| 代表者 | **妻**（開業届の名義人） |
| 所在地 | 開業届の住所 |
| 連絡先 | `tokyo.papa.home@gmail.com`（チラシ・HPに出している窓口） |

**前回はここが「個人・屋号なし」で出ていた可能性がある。**
審査は要するに身元確認なので、**照合できる届出と名前がそろっているかが効く。**
（理由は開示されないので、これも推測であることは変わらない）

### やること

- [x] ~~屋号つきの開業届を出す~~ ✅ **済み**（屋号ニコマル・妻）
- [ ] **開業届の控え／e-Tax受信通知を手元に出す**（Meta のビジネス認証にも同じものを使う → `docs/threads-api-review.md` §7）
- [ ] **ニコマル名義で再申請する**（上の表のとおりに揃える）
- [ ] ホームページに事業者情報を載せるかは、**通らなかったときに考える**
  （`tokushoho.html` はいま免責と個人情報のみ。**住所を公開するかは家庭の判断なので、先に出さない**）

> 💡 **急ぐ話ではない。**失っているのは「LINE内検索に出る」だけで、配信もQRも止まっていない。
> ただし**書類を探す手間はもう無い**ので、Meta のビジネス認証で控えを出すときに、
> **ついでに再申請してしまうのが一番安い。**
