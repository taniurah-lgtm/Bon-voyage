水曜の定期巡回。無料版「ぼんぼやーじゅ通信」を作って、LINEにブロードキャストする。

## 手順0 リポジトリを使えるようにする（いちばん最初にこれ）

**まず `add_repo` ツールを呼ぶ。** 引数は `owner: "taniurah-lgtm"` / `repo: "Bon-voyage"` / `access: "push"`。

🔴 **これを飛ばすと git は必ず403で落ちる**（2026-09-09に発生）。
定期実行のセッションは、リポジトリが authorized set に入っていない状態で起動することがある。
その状態では git プロキシが認証情報を注入しないので、clone も push も
`access denied by the git proxy ... 403` で失敗する。

`add_repo` を呼んだら、返ってきたパス（`/home/user/bon-voyage` など）を使う。
`status: already_present` なら再クローンしない。

```bash
REPO=$(ls -d /home/user/Bon-voyage /home/user/bon-voyage 2>/dev/null | head -1)
if [ -z "$REPO" ] || ! git -C "$REPO" rev-parse HEAD >/dev/null 2>&1; then
  git clone https://github.com/taniurah-lgtm/Bon-voyage /home/user/bon-voyage
  REPO=/home/user/bon-voyage
fi
cd "$REPO" && git rev-parse --show-toplevel && git branch --show-current
```

clone は5〜10分かかることがある。**タイムアウトを長めに取り、途中で諦めない。**

**`ls /` や `ls /mnt` や `find / -name .git` で探し回らない。**
無いのは置き場所の問題ではないので、探しても見つからない。承認する人も居ないので、
探索コマンドを打った時点で承認待ちになり巡回が死ぬ。
`add_repo` と上のコマンドが両方失敗したときだけ、**その出力の原文**を報告して終わる。

作業ブランチは `claude/family-event-planning-rfwmo8`。別ブランチにいたら
`git checkout claude/family-event-planning-rfwmo8 && git pull --rebase origin claude/family-event-planning-rfwmo8`。
コミット・pushはこのブランチだけ。PRは作らない。

★2026-09-18、ブランチを1本にまとめた。台帳もホームページもnoteも、すべてこのブランチにある。
他のブランチから何かを取り直す必要はない。

## 🔴 メールの受信箱は見ない（2026-09-09 決定）

小金井公園から報道発表がメールで届くが、**このRoutineはメールを読まない。**
Gmailのコネクタは意図的に付けていない。届く先は公開の問い合わせ窓口でもあり、
読者の私信が入るので、無人で動くセッションの読み取り対象にしない。

**メールで来た情報は、オーナーが事前に `events/` へ起票している。**
だから**台帳を読めば足りる。**受信箱を探しに行かない。

## この巡回の完了条件（重要）

**次のどれかに到達するまで、終わらない。**

- **(A) `scripts/publish_report.sh` が配信まで成功した**
- **(B) すでに今週ぶんが配信済みだと分かったので、送らずに記録だけ整えた**
- **(C) 途中で失敗し、エラー出力の原文とレポート全文を最終メッセージに載せた**

⚠️ **数分で終わった巡回は、失敗である。**
2026-09-02 の巡回は78秒、2026-09-09 の巡回は3分半で終了し、どちらも配信ゼロだった。
調査に時間がかかるのは正常。**「状況を確認して報告だけして終わる」は選択肢に無い。**
判断に迷ったら、**手を動かす側に倒す**。原稿は必ず作る。

## 手順1 今週ぶんが既に配信されていないか確かめる（最優先・30秒）

```bash
ls reports/free/
tail -5 reports/free/sent.log
curl -sS -H "Authorization: Bearer $LINE_CHANNEL_ACCESS_TOKEN" \
  "https://api.line.me/v2/bot/message/quota/consumption"
```

🔴 **二重配信の防止。** 水曜は09:00の巡回のほかに、11:30の見張りが動き、
さらに**オーナーとの対話セッションから手で送られることもある**（2026-09-09がそうだった）。

**次のどちらかに当てはまったら、LINEは絶対に送らない。**

- `reports/free/sent.log` の最後の行が**今日の日付**
- `totalUsage` が先週から**今週の配信ぶん増えている**

→ この場合は **(B)**。未コミットの変更があれば commit & push だけして、
`✅ <日付>号は配信済み（totalUsage=◯◯）。二重配信を避けて送信しませんでした。` と報告して終わる。

ファイルが無くても「配信されなかった」と決めつけない。**`totalUsage` が実配信の唯一のリアルタイム証拠。**

## 手順2 🔴 今日の号がすでに置かれていないか見る（最重要・2026-09-16 の事故）

```bash
ls -l reports/free/$(TZ=Asia/Tokyo date +%Y-%m-%d).md 2>/dev/null
```

### ファイルがあったら — それが正。書き直さない

**中身を書き直さない。上書き保存しない。**
オーナーと前日までに詰めた草案が置いてある。

**手を入れてよいのは、次の2つだけ。**

1. 🔴 **天気の行**（`docs/sources.md` の「🌦 天気」＝ **ウェザーニュース 小平市**
   https://weathernews.jp/onebox/tenki/tokyo/13211/ から**配信の朝に取り直す**）
   ★2026-09-20、tenki.jp は降ろした。**ウェザーニュースだけを見る。突き合わせない。**
2. **一推しが屋外で、取り直した降水確率が高く出たとき**は、台帳の屋内の手札に差し替える。
   このときも**差し替えた1枠だけ**を書き換え、ほかの枠には触らない

**イベントの取捨選択・順番・文面は触らない。**
🔴 **「自分ならこう書く」で書き直さない。**草案は人と詰めた結果で、
**載せないと決めたものまで復活してしまう。**

そのまま手順5（配信）へ進む。

> 🔴 **2026-09-16、合意ずみの草案があったのに、この確認が無かった。**
> Routine が自分の版を上書きで書いて配信し、**降水確率90%の連休にプールを薦めた。**
> 訂正と補足で、**読者64人に1日3通**送ることになった。**確認は30秒。省略しない。**

### ファイルが無いときだけ、以下で作る

1. `CLAUDE.md` / `docs/sources.md` / `docs/routines.md` / `docs/line-setup.md` /
   `profiles/free-hanakoganei.md` / `events/` の当月・翌月を読む
2. 台帳の「確度: 公式確認済み / 市報PDFで確定」の項目だけで骨格を組む。これで号は成立する
3. `reports/free/YYYY-MM-DD.md` に保存する。**下書きの指示ブロックを本文に混ぜない**

**この時点でファイルが存在していれば、最悪でも復旧できる。先に作ること。**

## 手順3 調査で厚みを足す（ファイルを自分で作ったときだけ）

`docs/sources.md` の「取り方の順番」と「コア巡回セット」に従う。日付は必ず該当年の公式で裏取り。

- 先2〜4週間の新規イベント
- 🔴 **探すものは季節で入れ替える。**`CLAUDE.md` の季節表を見て、
  **その2〜4週間に実在するもの**を探す
- **児童館（kodaji.ashita-ba.co.jp）** … 無料・屋内。土日祝も開館、休館は第1・第3火曜
- **公民館の講座一覧** … 「>>◯月◯日より詳細がご覧いただけます」を過ぎている行は個別ページを必ず開く
- **子ども家庭支援センターだより「おひさま」** … 0〜2歳枠の本命。PDFの1・2ページを両方読む
- **こだいら観光まちづくり協会（フラッとNAVI）** … 商店会・神社の祭りはここにしか載らない
- **こだいらNEWS バックナンバー**（https://plus.sugumail.com/usr/kodaira/doc）… 図書館・なかまちテラスの告知
- **小平市立公園 / ふれあい下水道館**（kodaira-parks.jp / kodaira-sewer.jp）…
  🔴 **月次イベントカレンダーは画像。**HTMLのテキストにイベント名が入らないので、画像を落として読む
- **市内の西側（小川・鷹の台・一橋学園圏）から最低1件**
- 西武新宿線ぞいの「電車でいけるおでかけ」を毎回1つ
- **0件だったソースは最終メッセージに1行ずつ残す**

## 手順4 原稿の決まりごと（ファイルを自分で作ったときだけ）

- タイトルは「ぼんぼやーじゅ通信」。宛先は未就学児・小学生のいるご家庭ぜんぱん
- **■今週の一推し** と **■イベント先取り情報** を柱にする（「急いで」「損」「今だけ」は使わない）
- 見出しに社内の言い回しを付けない
- 各イベントに年齢3カテゴリの目安（👶/🧒/🎒 を ◎○△✕）
- 🔴 **赤ちゃん（👶）が◎か○の枠を必ず1件入れる**
- **週末の天気を必ず反映**（**ウェザーニュース 小平市**。崩れそうなら室内を前に出し、館内イベント名まで）
- **📍地図リンクは「今週の一推し」1件だけ。**ほかのURLはLINEに載せない
- 🔴 **AI一覧の案内（「ChatGPTなどのAIをお使いの方へ」の2行）は入れない**（2026-09-20 取りやめ）。
  `/ai/` のページ自体は残すので `node scripts/build-events-json.mjs && node scripts/build-ai-pack.mjs` は回す
- 🔴 **分量の上限は 1,468文字 / 42行**（2026-09-20 決定。9/2便がその実寸）。
  **効くのは文字数ではなく行数。本文に使えるのは 1,155文字 / 33行 ＝ 本文3本まで。**
  4本目からは「イベント先取り情報」に2行で落とす。詳細は `docs/line-setup.md`
  ⚠️ **`wc -c` はバイト数。日本語では約2.5倍に出る。**文字数は python3 の `len()` で数える
- 末尾はホームページ導線フッターのみ。**価格・「応援」・「サポーター」・カレンダー登録の案内は書かない**
- **「無料」と書く前に費用を確認する**
- 🔒 **多摩六都科学館に触れたら**「※最新の情報は多摩六都科学館ウェブサイトでご確認ください。」を必ず添える。
  **小金井公園**なら「※最新の情報は小金井公園の公式サイト・Xでご確認ください。」を同じ強さで添える

## 手順5 配信（必ずこの1コマンド）

```bash
scripts/publish_report.sh reports/free/YYYY-MM-DD.md
```

事前検査 → 残枠の確認 → commit → pull --rebase 込みの push → **pushが通ったときだけLINE配信** →
送信記録の確認 → **配信の記録（`reports/free/sent.log`）を push**。

- **`scripts/line_report.sh` を直接叩かない**
- **pushが通らないままLINEを送らない**
- `sent.log` の push が通って初めて、ホームページの「最新号のプレビュー」が今の号に切り替わる。
  **手で `sent.log` に書き足さない**
- **無料枠を超えて止まった場合、勝手に `--force` を付けない。**残枠と必要通数を報告して指示を待つ

## 手順6 最終メッセージ（必須）

1. `add_repo` の結果
2. 🔴 **手順2の判定 — 今日の号のファイルが既にあったか／自分で作ったか。**
   **あった場合は「書き直していない」ことと、天気以外に触ったかどうかを明記する**
3. 手順1の判定（既に配信済みだったか）と `totalUsage` の実数
4. 今回の **生成 / push / 配信 / sent.logのpush** の成否を1つずつ明示
5. 0件だったソース
6. 失敗があれば**エラー出力の原文**とレポート全文
