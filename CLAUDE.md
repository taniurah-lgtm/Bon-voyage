# Bon-voyage — 家族イベント巡回システム

花小金井駅南側在住の5人家族(子ども: 年長・年中・2歳)のための、週末イベント・夏祭り・花火・プール・キャンプ情報の定期巡回リポジトリ。

家族の詳細は `profile/family.md` を必ず最初に読むこと。
複数ユーザー(無料パイロット)の運用は `profiles/README.md` と下記「複数ユーザー運用」を参照。

## 定期巡回の手順(水曜・土曜 朝のトリガーで実行)

0. **前回号の生存確認(最初にやる・30秒)**: `ls reports/free/` に前回の配信日のファイルがあるか見る。
   **無ければ「配信されなかった」ではなく「pushが落ちた」を先に疑う。**
   LINE側の送信記録で実際に届いたかを確かめてから報告する:

   ```bash
   curl -sS -H "Authorization: Bearer $LINE_CHANNEL_ACCESS_TOKEN" \
     "https://api.line.me/v2/bot/insight/message/delivery?date=YYYYMMDD"
   ```

   > ⚠️ **2026-08-19 訂正。** それまで「8/5・8/12は号が飛んだ」と書いていたが**誤りだった**。
   > LINEの送信記録を見たら **46通ずつ実際に配信されていた**。落ちていたのは push だけ。
   > **同じ型が 2026-07-22 / 07-29 にも起きている**(手順5の注記)。7月は手動で復旧、
   > 8月は気づかず放置。**これで4回目の同じ失敗。**
   >
   > **配信の証拠は LINE側の送信記録。**リポジトリのファイルは*記録*であって配信ではない。
   > **両方を見ないと状態が分からない。**
   >
   > 疑わしい原因: **非fast-forwardによる push 失敗**。毎朝07:00のSNS草案ワークフローが
   > 同じブランチにコミットするので、巡回(09:00)は**必ず後発**になる。
   > 手順5の `git pull --rebase` を**必ず実行する**。そして
   > **push に失敗したら LINE を送らずに中断して報告する**(送ってしまうと記録だけが消える)。

1. **台帳を読む**: `events/` 配下の当月・翌月ファイルと `events/camp.md` を読み、既知イベントとステータスを把握する。
2. **調査する**(Web検索。日付は必ず該当年のものを公式ソースで裏取り):
   - **巡回先は `docs/sources.md`(検証済みレジストリ)を起点にする**。「コア巡回セット」を毎回回す。
     取得は WebSearch主・WebFetch従(イオン/多摩六都/都サイトは直取り403のため検索で拾う)。
   - **水曜巡回**: 先2〜4週間の新規イベント発掘(夏祭り・花火・プール・季節イベント)+キャンプ場の空き情報。
   - **土曜巡回**: 当日・翌日のイベントの直前確認(中止・変更・天気)+翌週末の候補の再確認。
   - **月中旬(第2水曜など)**: 「映画・子連れ上映チェック」を走らせ、翌月の子連れ上映の確定日を拾って台帳に起票する(手順は `docs/sources.md`)。
     ⚠️ **「げんきッズシアター」は終了した可能性が高い**(2026-08 検証: イオンシネマ公式の該当ページが404、劇場ページにも記載なし。検索インデックスに旧ページが残っているだけ)。**名前で探さず、劇場ごとに現行の子連れ向け上映を確認する**。現時点で確認できているのは **T・ジョイ所沢の「音量小さめ・場内明るめ」上映**。ママズクラブシアター(TOHO系)の現況も未確認。**掲載前に劇場へ現況確認すること。**
   - **連休の先取り**: `docs/sources.md` の連休カレンダーを見て、2〜3ヶ月先の連休を、具体的な予約開始日つきで先取り提案する(例: 9月連休 × キャンプの予約解禁日)。
   - **西武新宿線ぞい**: 花小金井=西武新宿線。"電車でいけるおでかけ"(東伏見アイスアリーナ/田無/八国山・北山公園/西武園ゆうえんち/所沢サクラタウン・航空記念公園 等)を毎回1つは織り込む(車なし家庭にも刺さる)。
   - **市内の重みづけ**: **花小金井側を厚く、小平市西側(小川・鷹の台・一橋学園圏)を薄く**。ただし西側は毎回最低1件入れる(ゼロの号を出さない)。配分表と決め打ちソースは `docs/sources.md`「市内の地理的な重みづけ」。
   - 対象範囲: 小平市を中心に多摩地域全体+都心。近隣市の距離2段(コア/スポット)は `docs/sources.md` 参照。キャンプは関東近郊(車2.5時間圏)。
3. **台帳を更新**: 新規イベントを追加、終了・中止を反映。ステータスは `候補` / `承認済` / `登録済` / `終了` / `見送り`。
4. **レポート作成**: `reports/YYYY-MM-DD.md` に番号付きレポートを作成(フォーマットは下記)。番号は台帳のID(例: E12)をそのまま使う。
5. **配信は `scripts/publish_report.sh` 1本で行う**(手作業で分けない):

   ```bash
   scripts/publish_report.sh reports/free/YYYY-MM-DD.md
   ```

   このスクリプトが、事前検査 → 無料枠の確認 → commit → **pull --rebase 込みの push** →
   **push が通ったときだけ LINE配信** → 送信記録の確認 まで通す。

   > ⚠️ **なぜ道具にしたか**: 2026-07-22 / 07-29 / 08-05 / 08-12 の**4回**、
   > 「LINE配信は成功・pushだけ失敗」で記録が残らなかった。手順書には書いてあったが
   > 守られなかったので、**手順ではなくスクリプトに閉じ込めた**。
   > **push より先に LINE を送らないこと**が肝心。送ってしまうと、届いたのに記録が無い
   > 状態になり、あとから復元できない。

   - **失敗したら止まる**。push が通らなければ LINE は送られず、レポート全文と
     エラー原文が出力されるので、それをオーナーに渡す。
   - 無料枠を超える場合も止まる。承知のうえで送るなら `--force`。
   - `scripts/line_report.sh` を直接叩かない(検査と push を飛ばすことになる)。

6. レポート末尾に「Claudeに『E3とE5を登録』のように返信するとGoogleカレンダーに登録します」と案内する(**詳細版のみ**。無料版には入れない)。

## 承認 → カレンダー登録の手順

ユーザーが「E3とE5を登録」等（家族向けレポートでは「西武園の花火を登録」等のイベント名）と返信したら:

1. 該当イベントを Google_Calendar MCP の `create_event` で登録する。
   - タイトル: イベント名(例: 「小平グリーンロード灯りまつり」)
   - 場所・開始/終了時刻・説明欄に公式URL・📍Googleマップリンク・子連れメモを入れる
   - タイムゾーン: Asia/Tokyo。リマインダー: 前日18:00相当(popup, 前日)
   - 終日イベント(プール等の「この日行く」系)は allDay で登録
2. 台帳のステータスを `登録済` に更新し、カレンダーのイベントIDを台帳にメモする。
3. コミット&プッシュ。

## レポートのフォーマット

```
【ぼんぼやーじゅ通信 M/D(曜)】
■今週末のおすすめ
E3. イベント名 — M/D(土) 16:00〜 @場所
   ひとこと(子連れ適性・雨天時)
■ちょっと先の予定
E7. ...
■キャンプ空き情報
C2. ...
→ Claudeに「E3を登録」と返信でカレンダー登録
```

- LINEで読みやすいようプレーンテキスト、1通あたり最大でも40行程度。詳細URLは台帳とレポートファイルに残す。
- おすすめ順に並べ、2歳連れ適性(ベビーカー可否・混雑・オムツ規定)を必ずひとこと入れる。

## 複数ユーザー運用(無料パイロット)

`profiles/*.md` が存在する場合、上記の定期巡回を「共通調査 → 個別レポート」で行う:

1. **プロフィール読込**: `profiles/` の `active: true` を全部読む。各人の `coverage`(対象エリア)と `categories`(興味)を集計する。
2. **共通調査(1回)**: 全員の対象エリア・カテゴリの**和集合**で巡回し、共有台帳 `events/` を更新する。人ごとに調べ直さない(コスト削減の要)。友人は多摩・中央線沿線中心なので既存台帳を流用しやすい。
3. **個別レポート生成**: プロフィールごとに台帳を絞り込み・並べ替え、`reports/<id>/YYYY-MM-DD.md` に生成する。
   - `coverage` で範囲を、`categories` でジャンルを絞る。`camp_ok:false` ならキャンプ欄を省く。
   - `diaper_care:true` はオムツ/水遊びパンツ規定を明記、`stroller_needed:true` はベビーカー可否を必ず添える。
   - `weekday_ok:true` なら空いてる平日候補も入れる。`budget:free_first` は無料・格安を上位に。
   - `frequency:1x_week` の人は水曜のみ配信(土曜はスキップ)。
4. **配信**:
   - `delivery: line` → **`scripts/publish_report.sh`** で送信(オーナーはこちら)。`line_report.sh` を直接叩かない。
   - `delivery: manual` → レポート本文をチャットに出し、オーナーがLINE/メールで手渡し(パイロットの友人はこちら)。
   - `delivery: email` → メール送信(将来)。
5. **コミット&プッシュ**: 台帳・全 `reports/<id>/` をまとめて push。

※個人情報を含むためリポジトリは非公開のまま運用する。新規ユーザーの追加は `profiles/<id>.md` を1枚置くだけ(次回巡回から自動対象)。

## 配信の運用(無料の通信＋オーナー向け詳細版)

> ⚠️ **2026-09-04 方針転換。月額制度は廃止した。**現行の方針は `docs/strategy-2026-09.md`(これが正)。
> `docs/freemium-plan.md` ほか有料まわりのドキュメントは**廃止・凍結**。読者獲得に集中し、
> 収益は**スポンサー(BtoB)**で作る。個人からの応援は**読者300人を超えてから年3,000円**で始める。
> **それまで、通信にもホームページにもお金の話を出さない。**

「共通調査」は1回。生成・配信だけプロフィールごとに分ける。

- **トーン最重要**: 身内・友人も読む前提。**お金・申込を前面に出して急かさない**。無料通信は役立つ情報だけ、収益導線はホームページに預ける。
- **無料版**: `profiles/free-hanakoganei.md`(エリア共通・週1・水のみ)から `reports/free/YYYY-MM-DD.md` を生成し、無料LINEにブロードキャスト。
  - タイトルは「ぼんぼやーじゅ通信」。宛先は**未就学児・小学生のいる家庭ぜんぱん**(特定の子の年齢に寄せない)。
  - 各イベントに**年齢3カテゴリの目安**(👶あかちゃん/🧒未就学/🎒小学生 を ◎○△)。「ママ目線で厳選」等は使わない。
  - **週末の天気予報を踏まえた具体提案**を入れる(崩れそうなら室内＋その日の館内イベント名まで)。
  - 時限ネタは**「イベント先取り情報」**として淡々と事実で(「急いで」「損」等は使わない)。
  - **見出しは「■今週の一推し」。**`(決めてあげる)` のような**社内の言い回しを見出しに付けない**
    (2026-08 まで本文に混入し、ホームページにも出ていた)。`publish_report.sh` が検知して止める。
  - **📍地図リンクは「今週の一推し」1件だけ**に1行添える(例: `📍地図: https://www.google.com/maps/search/?api=1&query=施設名`)。ほかのイベントや出典URLはLINEに載せない(読みやすさ最優先。地図の全件・出典はホームページ/ガイド/台帳に置く)。
  - 末尾は**ホームページ導線フッター**のみ。**価格・「有料」・「応援」・「サポーター」は書かない。**カレンダー登録の案内も入れない。
- **詳細版(オーナー向け)**: `plan: paid` のプロフィールごとに `reports/<id>/YYYY-MM-DD.md` を生成(**週1・水曜**)。カレンダー登録案内フッター付き。
  > ⚠️ **`plan: paid` は課金とは無関係。**月額制度は 2026-09-04 に廃止した。
  > いまこの値が付いているのは**オーナー自身のプロフィールだけ**で、「地図・公式リンクつきの詳細版を作る」
  > という意味しか持たない。**読者に売っているものではない。**
  > ⚠️ **2026-09-02 に「週2・水土」から変更。**無料の週1配信が直近2か月で6回落ちており、週2は守れない約束だった。
  - **各おすすめに📍地図＋公式リンク**を添える(無料版のような1件制限はかけない)。
  - カレンダー登録時は説明欄に地図・公式URLを格納する(下記「承認→カレンダー登録」参照)。
  - **家族向けレポートには台帳ID(E3等)を出さず、登録はイベント名で受ける**(内部で台帳に突合)。
- 無料は週1に固定。**到達59人の時点で LINE 無料枠(月200通)を超えている**ため、
  ライトプランに課金するか、メール配信に移るかは**未決**(3案は `docs/strategy-2026-09.md`)。
- **課金を再開するとき**(読者300人・年3,000円)は、その前に特商法表記・免責を用意する
  (`docs/tokushoho-template.md`。ホームページの `tokushoho.html` は廃止時に免責ページへ作り替えた)。

## 協賛(有料掲載)の扱い

詳細は `docs/sponsor-plan.md`。**通信に載せるときの決まりだけ、ここに書く。**

- 🔴 **お金をもらった掲載には、必ず「協賛」と明記する。**景品表示法のステマ規制。
  方針ではなく法律なので、削らない
- **紹介型は1号1件まで。**冒頭に置かない。バナーを貼らない。
  書式は**ほかのイベントと完全に同じ**(年齢3区分・天気の目安・締切)にする
- **原稿はこちらが書く。**お店に書かせない(宣伝文になる)。
  ベビーカー可否・おむつ替え・予約の要否・何歳から・料金・締切を必ず入れる
- **応援型は名前だけ。**号のいちばん下に「この通信は以下の協賛で届いています」＋屋号のみ。
  **5枠まで**
- **同業種は同じ月に2件入れない**
- **断る基準**: 子連れに向かない先・読者に合わない業種は、**金額が良くても載せない**。
  市の窓口と多摩六都科学館に置いてもらえている信用は、1回の掲載で失える

## 運用ルール

- イベント日付は公式サイト・自治体サイトで裏取りする。未確定は「(要確認)」を付ける。
- **多摩六都科学館の情報を載せるときは、必ず「最新の情報は多摩六都科学館ウェブサイトでご確認ください」の一文を添える**(2026-09-04 に先方から依頼された掲載条件。共通フッターで代用せず、館名を明示してこの文言をそのまま書く)。詳細は `docs/sources.md`。
- 情報の正はこのリポジトリの台帳。カレンダーは承認済みイベントの写し。
- 秘密情報(LINEトークン等)は絶対にリポジトリにコミットしない。環境変数から読む。
- push先は `claude/family-event-planning-rfwmo8` のみ。PRは作らない(指示があった場合を除く)。
